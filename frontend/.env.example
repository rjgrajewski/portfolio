# Copy to .env.local for local development (git-ignored).
#
# Reasoning Lambda Function URL — the `AgentFunctionUrl` output of the
# `portfolio-api-<env>` CDK stack (docs/ARCHITECTURE.md § Real-time media
# transport). Leave unset to develop the frontend against the
# "assistant unavailable" fallback state.
VITE_AGENT_URL=

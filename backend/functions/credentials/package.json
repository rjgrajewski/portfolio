{
  "name": "@portfolio/credentials-fn",
  "private": true,
  "version": "0.0.0",
  "description": "Credential-vending Lambda — breaker-checked sts:AssumeRole for the scoped Polly/Transcribe media role (docs/ARCHITECTURE.md § Abuse protection — the OQ-8 fix).",
  "scripts": {
    "typecheck": "tsc --noEmit"
  },
  "//": "All @aws-sdk/* deps are devDependencies on purpose: they are marked externalModules at bundle time (backend/infra/lib/identity-stack.ts) and provided by the Node 22 Lambda runtime, so they are never bundled or shipped. They exist here only for local typecheck / editor support.",
  "devDependencies": {
    "@aws-sdk/client-dynamodb": "^3.700.0",
    "@aws-sdk/client-sts": "^3.700.0",
    "@aws-sdk/lib-dynamodb": "^3.700.0",
    "@types/node": "^22.10.0",
    "typescript": "^5.6.3"
  }
}

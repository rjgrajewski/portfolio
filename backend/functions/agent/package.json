{
  "name": "@portfolio/agent-fn",
  "private": true,
  "version": "0.0.0",
  "description": "Reasoning Lambda — Bedrock Haiku 4.5, streaming NDJSON over a Function URL (docs/ARCHITECTURE.md § Real-time media transport).",
  "scripts": {
    "typecheck": "tsc --noEmit"
  },
  "//": "All @aws-sdk/* deps are devDependencies on purpose: they are marked externalModules at bundle time (backend/infra/lib/api-stack.ts) and provided by the Node 20 Lambda runtime, so they are never bundled or shipped. They exist here only for local typecheck / editor support.",
  "devDependencies": {
    "@aws-sdk/client-bedrock-runtime": "^3.700.0",
    "@aws-sdk/client-dynamodb": "^3.700.0",
    "@aws-sdk/client-s3": "^3.700.0",
    "@aws-sdk/lib-dynamodb": "^3.700.0",
    "@types/node": "^22.10.0",
    "typescript": "^5.6.3"
  }
}

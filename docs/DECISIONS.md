# Decisions log

One line per decision, newest first within a date. The reasoning behind these lives in [ARCHITECTURE.md](ARCHITECTURE.md); this is the fast index of *what was decided and when*. When you make a new call that affects architecture, add a line here and update ARCHITECTURE.md.

Format: `YYYY-MM-DD — <decision> — <one-line why>`

---

## 2026-08-31 — Corpus interview pass

- 2026-08-31 — **Internal FlowJob nicknames (Argus, Minerva) banned from visitor-facing answers.** They survived the length pass: `flowjob.business.md` still closed with "Ask for the technical layer — Argus, Minerva…", and the live Lambda still had the pre-strip bundle (git push does not redeploy). The names are private, not recruiter language. Removed from the corpus closer; persona now forbids uttering them (say "the scraper" / "the skill-normalization pipeline"). Redeploy `portfolio-api-dev` and `portfolio-api-prod` so the bundled copy matches.
- 2026-08-31 — **Persona length made operational, not a preference.** "Tell me about FlowJob" produced a 4-paragraph briefing (origin + AWS stack including Argus/Minerva + a documentation-first lesson from another topic + a 3-way menu) despite "a few sentences, not an essay". Two causes: the soft line wasn't enough once `get_content` returned a full depth file, and the always-loaded FlowJob one-liner listed the stack so the model treated it as first-answer material. Fix in `systemPrompt.ts`: length is a hard 2–4-sentence cap on openers; **"tell me about X" does not fetch** (one-liners are enough; fetch only on a follow-up that asks for more); technical / origin / feature lists / funding / adjacent-topic lessons wait; one short fork, not a menu. Core FlowJob one-liner stripped of the stack list. The documentation-first lesson dropped from `flowjob.business.md` (it already lives in `portfolio-itself`). First pass still recapped the whole business file after fetching it; second pass forbids the fetch on the opener.
- 2026-08-31 — **Production git branch renamed `main` → `prod`.** Amplify URL follows the branch name, so production is now `https://prod.daz9bpic9q3nd.amplifyapp.com` (was `https://main.daz9bpic9q3nd.amplifyapp.com`). CORS on `portfolio-api-prod` / `portfolio-identity-prod` updated to match; GitHub default branch is `prod`; CI triggers on `prod` + `dev`. Matches the existing `dev`/`prod` CDK env names. The old `main` Amplify branch and git ref are removed after the new URL is live.

- 2026-08-31 — **Flagship corpus refreshed from a Claude.ai voice interview** — `core.md` plus amazon (business + technical), education/flowjob/portfolio-itself business now carry first-hand detail (termination & tenure tools, WFS Manager of three, BA document-compliance tool, Graphic Design specialization, flowjob.it started as a personal script, documentation-first build). Manifest one-liners updated to match; synced to the `dev/` S3 prefix; `portfolio-api-dev` redeployed so the bundled copy matches. Prod still on the previous pass until an explicit prod sync. `scripts/sync-content.ts` (and the Lambda bundling copy) now skip `.DS_Store`.

## 2026-08-30 — Phase 4 (voice I/O) + OQ-8 resolution

- 2026-08-30 — **Phase 4 promoted to production (owner's call, `dev`→`main`).** `portfolio-identity-prod` deployed for the first time — `portfolio-credentials-prod` (Function URL `https://sprkyfmf2mqtokdwipqtkojqam0vxrjp.lambda-url.eu-central-1.on.aws/`, `authType: NONE`, CORS locked to `https://main.daz9bpic9q3nd.amplifyapp.com`, `MEDIA_BREAKER_THRESHOLD: 120`) + `portfolio-media-guest-prod` (trust policy names only the vending Lambda's role; scoped to exactly `polly:SynthesizeSpeech` + `transcribe:StartStreamTranscriptionWebSocket`). `portfolio-api-prod` redeployed: new Lambda bundle (guardrail-4 systemPrompt + `KNOWLEDGE_BOUNDARY` + shared `breaker.ts`) and `SESSION_MESSAGE_CAP` 40 → 150 — no infra/permission/table changes in the diff. `VITE_CREDENTIALS_URL` set on the `main` Amplify branch to the prod credentials Function URL (overriding the app-level `VITE_CREDENTIALS_URL: "disabled"` default, which the new `runtime.ts` would otherwise treat as a literal URL). Order: identity-prod → api-prod → set the branch var → verify the prod endpoint (vends real STS creds; CORS allows the prod origin, denies a rogue origin) → merge `dev`→`main` → Amplify `main` build #5 (BUILD/DEPLOY/VERIFY green). Verified against `https://main.daz9bpic9q3nd.amplifyapp.com`: portfolio + orb render, a text turn streams a grounded answer from `portfolio-agent-prod`, an orb tap enters voice mode and `runCapture` vends from the **prod** credentials Lambda (confirmed by the fetch target — not dev, not `disabled`) with Transcribe streaming opening and closing cleanly, zero console errors. `dev` fast-forwarded to `main` so the two branches match. Owner-only remaining: the spoken round-trip + acoustic barge-in loop on a physical phone.
- 2026-08-30 — **"Voice regression" was the dev media breaker tripping, not a code change — plus a real UI bug it exposed.** After the orb/transcript-bar commit, on-device voice stopped starting and the bar showed a clipped "Voice is unavailable right now. You can ke…". Investigated by diffing `c5bd262..HEAD`: that commit touched only `App.tsx` (vizaudit branch), `AgentOverlay.tsx` (`size` 96→104), `AgentVisualization.tsx`, `TranscriptBar.tsx`, `DECISIONS.md` — **nothing under `frontend/src/agent/`, `frontend/src/config/`, or `backend/`**. Real cause: `portfolio-usage-counters-dev` → `media#day#2026-08-30` hit `requests: 58` vs the dev `mediaBreakerThreshold: 50` (Lambda logs: `media breaker tripped: 51/50 … 58/50`, first at 19:47Z — 4 min after the commit, mid device-test). Each credential vend counts (creds are memory-only, so every page load re-vends), and a day of on-device testing + the assistant's own verification runs crossed 50. Fixes: (1) **dev `mediaBreakerThreshold` 50 → 200** (matches the dev *reasoning* breaker; Polly TPS quota + the $25 AWS Budget alarm stay the hard backstops; the dev Function URL is CORS-scoped + unadvertised). Prod unchanged at 120. Deployed `portfolio-identity-dev`; the endpoint vends again (verified). (2) **The clipped, unexpandable notice** in `TranscriptBar`: a voice-availability notice with no conversation yet was rendered only as a `truncate`d one-liner in the collapsed pill, and the "History" expand control only exists when `messages.length > 0`, so there was no way to read the rest. Now, when the collapsed line IS a degradation notice (`lineIsNotice` — no live transcript to preview), it renders wrapped and in full (`whitespace-normal break-words`, `role="status"`, dot top-aligned); transcript lines still `truncate`; the notice is shown in exactly one place (pill OR the above-bar line, never both). (3) **The real cause is now in the console**: `[stt]`/`[tts]` log the specific `MediaCredentialError.code` (`media_breaker_tripped` / `media_throttled` / …) before the UI collapses them all to one `credentials_refused` notice; the `[voice]` line points at the preceding `[stt]`/`[tts]` line. Verified in the 375 px pane: voice starts from an orb tap and captures (real vend, Transcribe opens, no `credentials_refused`); a forced breaker-tripped vend shows the full two-line notice + a `Type` chip, no ellipsis.
- 2026-08-30 — **Orb double-outline removed; halo hard-clamped inside the canvas; transcript bar restyled; `?vizaudit=1` harness deleted.** On-device the speaking orb showed **two** concentric rings and the outer one clipped at the canvas edge — the iteration-3 halo ring had been *added alongside* the blob's own perimeter `ctx.stroke()` instead of replacing it, and at peak amplitude its radius (`maxR·1.62 ≈ px·0.55`) exceeded the canvas half-width. Fix in `AgentVisualization.drawBlob`: the blob is now a **gradient fill only, no stroke** — a brighter warm core (`rgba(255,214,150,·)` centre) so it still reads as a solid form without an edge — leaving the halo ring as the **single** ring; every drawn radius (blob points, corona, ring) goes through `R(m) = Math.min(px·0.47, maxR·m)` with `maxR` cut `0.34 → 0.30` and the ring's amplitude reactivity `0.22 → 0.12`, so nothing can ever be drawn past the edge. Canvas bumped `96 → 104 px`. **Verified in the 375 px pane, speaking state with a section open**: over a real speaking burst with every `AnalyserNode` saturated to full scale, the outermost 2 px border of the canvas stayed fully transparent (max alpha 0) and the furthest drawn pixel was 91 px from centre in a 104 px half-canvas — a 13 px margin at peak, no clip. The **transcript bar** (`TranscriptBar`) was a `rounded-t-xl border-b-0 bg-neutral-950/90` cut-off card with plain-text `▴`/`▾` buttons — now a fully-enclosed `rounded-2xl` card on `bg-neutral-900/85 backdrop-blur`, an amber status dot while voice is live, and chip-style controls (bordered pills, amber-on-hover) for Type / History / Collapse; the expanded panel and the `Composer` wrapper use the same card. The `_VizAudit.tsx` component and the `?vizaudit=1` branch in `App.tsx` (temporary iteration-3 harness) are **deleted** — the plain URL is the only view. Barge-in, z-index layering, section reveal (agent + tap), and orb-tap-to-exit all re-verified unaffected.
- 2026-08-30 — **Agent guardrail 4 added: no candidacy assessment, no skill-gap claims.** Asked about a technology outside the corpus (Microsoft Copilot Studio), the agent had told a recruiter Rafal was "probably not the right fit for this role" and stated "isn't something he's worked with" as fact. Two systemic errors: (1) a fit verdict, which is the recruiter's call, not the agent's; (2) asserting a skill gap it cannot know — the corpus is a *selection* of career topics, so absence from it is the agent's knowledge boundary, not evidence Rafal lacks a skill. Fix in `systemPrompt.ts`: guardrail 4 forbids any fit / suitability / seniority / comparison / rating / "could he handle" verdict and any "he hasn't worked with X / doesn't know Y" assertion (no guessing either way); the Grounding section now says explicitly it "cuts both ways"; a new `KNOWLEDGE_BOUNDARY` block defines the 3-part answer for out-of-corpus tech — *(a)* "I don't have information about that" *(b)* nearest real work from the corpus, described for what it is, not contrasted with the asked-about thing *(c)* referral to Rafal. Deployed to `portfolio-api-dev`; 10 cases (Copilot Studio, Azure, Kubernetes, Java, "senior AI engineer?", "years of Python", candidate comparison, "lead a team of five?", "rate his AWS 1-10", plus a fit-bait variant) verified — no fit verdict, no skill-gap claim in any. Catalogued in `docs/prompt-injection-tests.md` (new file, first Phase-7 category).
- 2026-08-30 — **AI-mode glow rebuilt (iteration 3) + orb gets a categorical listening state.** The frame turned out to be the *primary* signal for "a conversation is live" vs "the orb is waiting for a tap" (rest and listening-in-silence look alike), so "subtle" was the wrong brief — it's an app-state indicator, not decoration. (1) The frame is now a **radial-gradient edge vignette** (`transparent 46% → amber 100%`, `ellipse 118% 112%`) plus a tight bright rim, not an inset line/blur — light spilling from every edge, fading to *clean* in the centre (a plain inset blur overlapped across a 375 px phone and washed the whole screen; the gradient scales with the viewport aspect). Its opacity is driven per frame by `AgentOverlay` (slow breathe + a lift with mic/playback amplitude). (2) `AgentVisualization`: the "conversation live" states (listening / thinking / speaking) each get a **halo — a soft corona + an orbiting ring at a distinct faster pulse** — that rest and error have *at all*; rest is just a small quiet dot. So rest → listening is obvious at a glance even in total silence (verified in `?vizaudit=1`). (3) Halo and glow both react to the same `micLevel`/`playbackLevel`, so the orb and the screen edge move as one system. Amber palette kept; motion still only communicates state. `_VizAudit.tsx` + a `?vizaudit=1` branch in `App.tsx` are a **temporary** side-by-side harness — remove after review. Reveal / z-index / orb-tap-exit / barge-in all re-verified.
- 2026-08-30 — **Bottom voice zone simplified to two things: the orb and the transcript bar.** It had five (orb, state caption, "Leave voice" button, transcript bar, Safari's own bar). (1) The visible state caption ("Speaking" / "Listening" / …) is **removed** — the four blob states were designed to read on sight; a caption meant the animation wasn't carrying its weight. The state is still exposed to screen readers via a visually-hidden `aria-live="polite"` `role="status"` region (announced on change, silent at rest to avoid chatter); the button keeps a stable `aria-label`. `prefers-reduced-motion` static states were spread further (stepped `frozenLevel` 0 / 0.15 / 0.55 / 1 + a solid amber core for `speaking`) so the four are still distinguishable frozen. (2) The **"Leave voice" button is removed** — a tap on the orb ends voice mode (voice-assistant convention). Collision guard: `exitVoiceMode` also aborts any in-flight turn + stops playback; **barge-in is a separate path triggered by *speaking*, never by a tap**, so "tap = leave" and "speak = interrupt" can't be confused. A one-time transient hint ("Tap the orb again to leave", `sessionStorage`-gated, 4.5 s fade) covers discoverability; `active:scale-90` gives press feedback. No permanent text re-added. (3) Dock is now ~132 px (transcript bar 38 + gap 8 + orb 76 + `padding-bottom: max(0.6rem, env(safe-area-inset-bottom))`) — order flipped to transcript-then-orb so an expanding transcript grows upward and the orb stays put at the bottom edge with safe-area clearance for Safari's bar. Verified in the 375 px pane: orb stays above an open section, degradation (mic-denied → notice + working text) and barge-in paths unaffected.
- 2026-08-30 — **Agent presence fixed to sit ABOVE an open section; z-index consolidated; dock portaled to `<body>`.** On-device the new UI had three bugs with one-and-a-half causes: (2) the AI-mode frame was invisible and (3) the visualization + frame vanished when the agent opened a section — both because they were `z-30`, under the `z-40` mobile section takeover (confirmed by hit-test: the canvas centre returned the takeover's content div). (1) the visualization drifted on scroll on iOS despite `position: fixed` (not reproducible in the emulated pane — an iOS compositing quirk). Fixes: a single **z-index scale** as `:root` CSS custom properties in `styles/index.css` — `--z-header: 20`, `--z-section-takeover: 40`, `--z-ai-frame: 50`, `--z-agent-dock: 60` — read by `Header`, `MobileSectionOverlay`, `.ai-frame`, and the dock (no more scattered `z-30`/`z-40`); the agent dock + frame **rendered through `createPortal(..., document.body)`** so no ancestor that becomes a containing block (transform/filter/contain/backdrop-filter) can ever re-base their `fixed` positioning; `transform: translateZ(0)` on both so iOS Safari composites them as stable layers. The frame is also thicker/brighter (2 px @ 0.5, brighter still on listening/speaking) — the old 1 px @ 0.28 was below the perceptibility threshold on a phone. `MobileSectionOverlay`'s scroll container gets `pb-40` so the now-on-top visualization doesn't hide the tail of section content. Verified in the mobile pane: dock stays pinned through a page scroll; the canvas hit-tests as on-top with a section open via **both** the agent path and a header tap (one reveal path, unchanged).
- 2026-08-30 — **Agent UI rebuilt: voice-first, chat column removed.** The docked two-zone chat layout (fixed 380px agent column beside a scrolling content column) made a "voice portfolio" read as a website with a bolted-on chatbot. Replaced with: a **single scrolling column** that is entirely the portfolio (mobile and desktop alike — the `.app-layout` CSS grid is gone); a fixed **organic visualization** at the bottom edge (`AgentVisualization` — canvas + `requestAnimationFrame`, no animation library, amber/neutral palette, an organic blob not a sphere) present from first paint and tappable to start; four states legible without text — **rest** (slow breath), **listening** (swells with real mic RMS, read from a second analyser tap on the voice session's existing `sourceNode` — no extra stream), **thinking** (a lobe orbits the perimeter — rotational, unmistakably not listening's radial swell), **speaking** (fine shimmer pulsing with an `AnalyserNode` on the `tts.ts` output path); a subtle **AI-mode frame** (`.ai-frame`, fixed inset box-shadow, z-30) only while a conversation is live; the **transcript hidden by default** in a slim bottom bar (`TranscriptBar`), expandable to history, capped at 40vh and auto-collapsed whenever a section opens so it never buries content (z-30, below the z-40 mobile section takeover); a discreet always-present **"Leave voice"**. `prefers-reduced-motion` → the four states are drawn statically (distinct size/brightness/silhouette), no rAF. Perf: one small canvas, no per-frame allocation, paused while `document.hidden`; the audio thread always wins.
- 2026-08-30 — **Section reveal is untouched — one code path, no exceptions.** `revealSection` / `activeSectionStore`, the desktop accordion (`PortfolioSections`, unchanged) and the mobile full-screen takeover (`MobileSectionOverlay`, unchanged) all still drive it. The new agent presence is layered *over* the portfolio and only READS `useActiveSection`. Verified in-browser: `revealSection('flowjob')` opens the mobile takeover exactly as before; text turns stream end to end through the new dock; mic-denied degrades to the transcript bar with text still working; barge-in / degradation logic unchanged.
- 2026-08-30 — **Text input kept reachable, deliberately NOT restyled.** A small "Type" toggle in the transcript bar reveals the existing plain `Composer`. Whether typing lives inside voice mode or is an exit from it is a post-device-test decision, so it stays visually separate from the voice aesthetic for now.
- 2026-08-30 — **Per-session message cap: fresh `sessionId` every page load (was persisted in `sessionStorage`).** The old copy — "reload the page for a fresh one" — was a lie: `sessionStorage` survives a reload, so the cap stayed hit and the visitor was stuck. The cap is explicitly not an abuse control (§ Abuse protection — the id is client-side, rotates for free; the daily breaker is the real bound), so persistence bought nothing and only broke real users. Now `sessionId.ts` generates one id per load, memory only — the copy is true. Message reworded to be calm and ordinary ("That's a good run of questions for one visit. Reload the page whenever you want to keep going.") — the old wording read like an outage or funds running out. Cap loosened: dev 20 → 60, **prod 40 → 150** — a genuinely engaged recruiter (business + technical, per project, across four projects + STAR) can pass 40; the daily breaker (500) is the cost barrier, a tight session cap only risks cutting off the right person. Deployed to `portfolio-api-dev`; prod value in code, not deployed.
- 2026-08-30 — **Barge-in: voice input is now a persistent hands-free session, not push-to-talk-per-turn.** Voice worked end to end on staging but the mic went dead while the agent spoke — "listen to the answer", not "have a conversation". `stt.ts` is rewritten as `createVoiceSession`: ONE `getUserMedia` + ONE 16 kHz AudioContext + ONE `pcm-chunker` worklet held for the whole voice-mode lifetime, with two internal states — `monitoring` (worklet runs, VAD watches for a speech onset, a 500 ms pre-roll ring buffer is kept) and `capturing` (Transcribe WS open). The visitor can start talking at any moment, including while the agent plays; a sustained onset fires `onSpeechStart("barge-in")` and the session flips straight to `capturing` with the pre-roll prepended (first word isn't clipped). The caller then aborts the agent stream, `player.stop()` (epoch cancels queued synth), flags the partial answer `interrupted` (a flag, never in `text`, never in model history — like `truncated`), and the UI swaps speaking→listening immediately.
- 2026-08-30 — **Self-interruption (mic hears the agent through a phone speaker, no headphones) — layered defence.** `echoCancellation` + `noiseSuppression` on; an **adaptive trigger** = `max(speechRms, echoFloorEMA * 3.0)` where the echo floor is measured live (seeded fast during a guard window, then a lower-envelope follower that steady echo pulls up to itself but a burst of user speech does not); a **300 ms sustain** (3 chunks) that rejects taps/clicks/single loud syllables; and — **the loop protection chosen** — a **700 ms guard window after every playback start during which barge-in is disarmed** and the echo floor is seeded. Even if `echoCancellation` does nothing on a device, the agent gets 700 ms of protected speech and by the time barge-in arms the trigger sits at 3× that device's actual residual-echo level. All levels are logged (`[voice] levels rms=… thr=… echoFloor=… armed=…`) every ~900 ms *during agent speech* for cabled-console tuning. The VAD is pure (`frontend/src/agent/vad.ts`) and unit-tested by `npm run verify-vad` (11/11) — including "steady 0.02 and 0.045 echo never self-triggers" and "guard blocks a trigger even at full volume". **A prior EMA-from-sub-threshold-samples-only design was caught by that test self-triggering on any echo above the initial threshold and was replaced with the seed + lower-envelope tracker.**
- 2026-08-30 — **Don't-break-what-works, verified:** typing while the agent speaks still works (composer isn't disabled during playback); sending a text question still interrupts speech (`runTurn` stops the player); a voice failure still never disables the text composer. The mic button is now a persistent voice-mode toggle (on/off), enabled at any time, not a one-shot push-to-talk.
- 2026-08-30 — **TTS never ran: `createSpeechPlayer`'s stop-latch was one-way.** Input voice worked, the agent answered, but there was zero traffic to Polly and no error. Cause: `stop()` set `stopped = true` and nothing ever cleared it, while `useConversation.runTurn` called `player.stop()` at the top of every turn (and `startVoice` called it again). So by the first text frame, `push()` / `end()` / `runSynth()` all early-returned on the latch and the synth path silently never executed. The in-pane tests never caught it because with no mic, `runTurn({speak:true})` is never reached. Fix: a spoken answer is now an explicit **utterance** — `begin()` halts any prior playback AND clears the latch (`stopped`/`ended`/buffers/`announcedStart`), `runTurn` calls `begin()` instead of `stop()` for a spoken turn, `startVoice` no longer pre-stops the player. An **epoch counter** (bumped by `begin()`/`stop()`) makes a late Polly response or `decodeAudioData` from a superseded utterance drop instead of playing over the new answer. `stop()` stays the hard halt for error/teardown/switch-to-text. Verified from a real browser: `begin → sentence-chunk → getMediaCredentials (cached from STT's vend) → real Polly SynthesizeSpeech (14 KB + 10 KB mp3) → decodeAudioData → scheduled back-to-back → onStart/onIdle`.
- 2026-08-30 — **TTS + voice-turn logging added** (`[tts] …`, `[voice] …`), mirroring the `[stt]` lines — begin, each queued sentence, each Polly request + byte count + failures, each scheduled chunk with its offset/duration, flush, idle. So the next voice regression is diagnosable from the console, not by inference.
- 2026-08-30 — **Guest role's Transcribe action corrected to `transcribe:StartStreamTranscriptionWebSocket`; the Node HTTP/2 `transcribe:StartStreamTranscription` removed.** The browser SDK reaches Transcribe streaming over a *presigned WebSocket*, which is a **separate IAM action** from the HTTP/2 form the Node SDK uses — permission to one does not cover the other. `portfolio-media-guest-<env>` had only the HTTP/2 form, so browser STT failed with `AccessDeniedException … not authorized to perform: transcribe:StartStreamTranscriptionWebSocket` (after a successful `101` handshake — SigV4 validity ≠ action authorization). The HTTP/2 form was dropped rather than kept alongside: these credentials are used **only** from the browser (the whole media path is browser-direct), nothing server-side uses them, so granting the server variant would widen scope past what runs. Role still has exactly two actions, both matching real browser calls. Deployed to `portfolio-identity-dev`; prod not deployed.
- 2026-08-30 — **Polly checked for the same transport trap before deploying — none.** `polly:SynthesizeSpeech` is a plain SigV4 POST with no WebSocket/streaming IAM variant (the generative engine is a request parameter, not an action); `tts.ts` uses only `SynthesizeSpeech`. `StartSpeechSynthesisStream` (OQ-4) is not used. So `polly:SynthesizeSpeech` alone is sufficient for the browser — verified by a real call with the vended creds. No second deploy round needed.
- 2026-08-30 — **Verification lesson: prove IAM against the actions the *actual client* uses, transport variants included — not a server-side stand-in.** The first OQ-8 pass proved the security properties correctly (no unsigned path, no `AssumeRole` bypass, everything outside the grant denied) but its *functional* proof ran from Node, which uses `transcribe:StartStreamTranscription` (HTTP/2) — the exact action the role happened to have — so it went green while the browser's `…WebSocket` action was missing. Fix: `scripts/verify-oq8.ts` (`npm run verify-oq8`) now asserts, via `iam simulate-principal-policy`, that the browser's real action names are allowed and a broad denylist (including the Node HTTP/2 variant) is denied — plus the credential vend, a real Polly call, the `AssumeRole` bypass, and the breaker refusal. 8/8. The browser-only end-to-end WebSocket round-trip stays a live-staging check (the SDK's WebSocket transport isn't reachable from raw Node).
- 2026-08-30 — **STT capture rewritten to an AudioWorklet resampling to exactly 16 kHz** (`frontend/src/agent/pcmChunker.ts` + `stt.ts`). First staging test: Transcribe accepted the handshake then dropped the session ("the audio doesn't match the parameters you provided") because the browser captured at 44.1/48 kHz (Safari ignores the `getUserMedia` sample-rate constraint) while the request declared `sample-rate=16000`, and the old code only did a crude per-ScriptProcessor-block decimation. Now: `AudioContext({ sampleRate: 16000 })` requested (and the real `sampleRate` logged), then a worklet does continuous linear-interpolation resampling with fractional-position + input-tail state carried across quanta (pass-through at 16 kHz), emitting s16le mono PCM in 100 ms chunks. Chosen over `OfflineAudioContext` (fixed-length render → one instance per block, async, no cross-block state — the artifact returns) and over keeping `ScriptProcessorNode` (deprecated, main-thread, flaky on Safari 26). `npm run verify-resampler` proves duration + pitch preservation and s16le validity in Node (11/11). Live mic pass on staging still outstanding.
- 2026-08-30 — **OQ-8 resolved and retired: no Cognito Identity Pool — a breaker-checked credential-vending Lambda instead.** A Cognito guest role is handed out by `GetId` + `GetCredentialsForIdentity`, both **unsigned** calls that need only the public pool id, so there is no point at which a spend breaker can gate issuance — exactly the hole OQ-8 named, and Cognito cannot close it (disabling unauthenticated identities would force real login, which the product rules out). `backend/functions/credentials/` (Function URL, `authType: NONE`, CORS locked to the same origins as `api-stack`) is now the **only** way to get media credentials: in-container token bucket → daily **media** circuit-breaker (a counter INDEPENDENT of the reasoning breaker — `pk = media#day#<date>` in the same `usage-counters` table, per-env `mediaBreakerThreshold` = 50 dev / 120 prod) → `sts:AssumeRole` on `portfolio-media-guest-<env>`. Tripped breaker → `{ ok:false, code:"media_breaker_tripped" }` and zero credentials. `identity-stack.ts` is the home for both the vending Lambda and the guest role.
- 2026-08-30 — **The media-guest role is scoped to EXACTLY `polly:SynthesizeSpeech` + `transcribe:StartStreamTranscription`** (`Resource: "*"` — neither action supports resource-level permissions), and its trust policy names ONE principal: the vending Lambda's execution role. **Empirically verified on dev**: vended creds can `SynthesizeSpeech` (generative Ruth) but are `AccessDenied` for `polly:DescribeVoices`, `transcribe:ListTranscriptionJobs`, `s3`, `dynamodb`, `bedrock:InvokeModel`, and re-`AssumeRole` of themselves; a **direct `AssumeRole` of the guest role as full account admin is `AccessDenied`** (trust policy doesn't list admin); flipping the media threshold to 0 makes the endpoint refuse with `media_breaker_tripped` and issue nothing. There is no credential path that bypasses the breaker.
- 2026-08-30 — **Credential TTL is 900s — the STS `AssumeRole` floor, deliberately the minimum.** `DurationSeconds` can't go below 900 with temp creds, and a shorter window is strictly better here: a leaked set is useless after 15 min, a real voice conversation is minutes (the frontend re-vends transparently near expiry), and the media breaker bounds total grants/day regardless. A longer TTL would only widen the leak window for no benefit. Residual risk (accepted, documented in ARCHITECTURE.md § Abuse protection): within a live 15-min window the creds are unbounded on characters — Polly generative's low per-account TPS quota + the media breaker's grant cap + the $25 AWS Budget alarm are the bounds; full elimination needs a media proxy (plan B), deferred.
- 2026-08-30 — **Breaker logic extracted to `backend/functions/shared/breaker.ts`** (+ `shared/ddb.ts`, `shared/tokenBucket.ts`), used by BOTH Lambdas — not duplicated. `agent/src/breaker.ts` / `throttle.ts` / `ddb.ts` are now thin env-binding shims; the reasoning path's DynamoDB key (`day#<date>`) and behaviour are byte-for-byte unchanged (re-smoke-tested on dev after the refactor). `backend/functions/credentials` is a new npm workspace; `backend-ci.yml` type-checks it too.
- 2026-08-30 — **OQ-4 resolved and retired: Polly bidirectional streaming exists but is NOT browser-reachable → sentence-chunked `SynthesizeSpeech`.** `StartSpeechSynthesisStream` (announced 2026-03, generative-only, HTTP/2 event streams) is in the JS v3 SDK, but its request-side event stream needs Node's HTTP/2 handler — browsers can't open raw HTTP/2, `fetch` isn't full-duplex, and there is no Polly WebSocket fallback (unlike Transcribe). Since Bedrock is never called from the browser there is no server in the media loop to run it. `frontend/src/agent/tts.ts` therefore cuts complete sentences off the streaming answer, synthesises sentence *n+1* while *n* plays (Web-Audio-scheduled, pipeline depth ~1), and fires the first sentence the instant it's complete — audio starts while the answer is still streaming and lands with the section reveal, not after. Full write-up: `docs/voice-notes.md`.
- 2026-08-30 — **English voice: `Ruth` (en-US generative).** Chosen from the confirmed Frankfurt roster for a calm, even, credible narration register that survives sentence-by-sentence concatenation and pairs tonally forward with the PL `Ola`/`Ewa` for Phase 5. Resolves the English half of **OQ-7** (the PL pairing is confirmed in Phase 5). Alternates: `Amy` (en-GB), `Danielle` (en-US). One constant in `tts.ts`. Rationale in `docs/voice-notes.md`.
- 2026-08-30 — **Voice is a layer on top of text, sharing one turn machine.** `useConversation.ts` owns both: a spoken turn is an ordinary turn whose reply deltas are *also* piped to Polly; voice input (`stt.ts`, Transcribe streaming, `en-US`, silence-ends-the-turn) resolves to a transcript submitted through the identical path — so a visitor can ask by voice then type the next question in one conversation. Every voice failure (`mic_denied` / `mic_unavailable` / `transcribe_failed` / `polly_failed` / `credentials_refused`) is surfaced by `degradation.ts` as a separate, quieter notice that **never** disables the text composer or changes `canRetry`. Verified in-browser: mic-blocked → notice shown, text keeps working before and after.
- 2026-08-30 — **`degradation.ts` gains a second, independent verdict for voice** — `deriveDegradation({ configured, errorCode, voice })` → adds `voice: { available, notice }`. Text-agent modes/copy/`canRetry` are unchanged. `VITE_CREDENTIALS_URL` unset → `voice.available` false → the mic is simply not rendered; text is untouched. Every voice notice ends by pointing back at text — never a dead end.
- 2026-08-30 — **Mobile: mic press is the autoplay-unlock gesture; keyboard-covers-input handled in `Composer.tsx`.** `startVoice()` calls `player.unlock()` (resume AudioContext + play a 1-sample buffer, the iOS requirement) from inside the mic-button click, so the spoken reply plays without a gesture of its own. On focus / `visualViewport` resize the composer `scrollIntoView({ block: "center" })` so the on-screen keyboard never covers the input or the latest turn.
- 2026-08-30 — **`portfolio-identity-dev` deployed** (`cdk deploy portfolio-identity-dev --context env=dev`) → `CREATE_COMPLETE` in `eu-central-1`. Creates `portfolio-credentials-dev` (Lambda + Function URL `https://h5f7g2q5bbujgzmyzoohy6sp4u0luglm.lambda-url.eu-central-1.on.aws/`) and `portfolio-media-guest-dev`. `portfolio-identity-prod` is defined in `bin/app.ts` but **NOT deployed** — promotion is a separate, deliberate call. `guardrails-*` stacks still undeployed (empty skeletons).

## 2026-08-30 — Phase 3 (mobile), Phase 6 (demo-scope degradation), production cutover

- 2026-08-30 — **Manual section content is authored from `content/topics/*.md`, not written independently.** The manual click-through and the agent must never state different facts, so both draw from the same source; the manual components (`frontend/src/components/portfolio/sections/*.tsx`) just present it in a scannable visual form (short blocks, bolded specifics) instead of the corpus's spoken-register prose. `PlaceholderNote` removed from Education / Amazon / FlowJob / PortfolioItself.
- 2026-08-30 — **Fixed: `PortfolioItself.tsx` and `Footer.tsx` claimed Amazon Polly and Transcribe were running.** They are not — voice is Phase 4, unbuilt. Both now describe only what runs today (Bedrock Haiku 4.5, the `reveal_section` agentic UI, tool-fetch retrieval, the cost ceiling) and name voice as a later phase.
- 2026-08-30 — **`SECTION_CONTENT` extracted to `frontend/src/components/portfolio/sectionContent.ts`** — one map, imported by both the desktop in-place list and the mobile overlay, so the two presentations cannot render different content for the same section.
- 2026-08-30 — **Mobile section reveal is a full-screen takeover (`MobileSectionOverlay.tsx`); desktop stays the in-place accordion.** One reveal state (`content/activeSectionStore.ts`, unchanged) — a new `useIsDesktop` hook (`useSyncExternalStore` over `matchMedia("(min-width: 1024px)")`, the same breakpoint as Tailwind `lg:` and the `.app-layout` grid switch) only chooses the *presentation*. This is a deliberate, narrow exception to the "CSS-only switch, no `useMediaQuery`" call from the two-zone-layout work: mounting/unmounting a `position: fixed` overlay, locking `body` scroll, Esc-to-close and focus management genuinely need JS, and this hook does not fork layout structure the way the earlier concern was about.
- 2026-08-30 — **The mobile overlay is read-only against the reveal store** — it only calls `closeActiveSection` (exit) and `revealSection` (its "More" list), never a mobile-specific open path. So the agent's `reveal_section` tool opens the takeover exactly like a tap, and because nothing in the agent zone unmounts, the transcript is intact when the overlay closes. Verified live (tap **and** agent-triggered) on desktop and a 375×812 viewport.
- 2026-08-30 — **`PortfolioSections` `scrollIntoView` is now desktop-only** — on mobile the full-screen overlay *is* the "bring into view"; scrolling the list behind it would be pointless motion.
- 2026-08-30 — **`frontend/src/agent/degradation.ts` is the single source of truth for agent availability** (Phase 6, **demo scope only**). `deriveDegradation({configured, errorCode})` → `{mode, notice, canRetry}`. Deliberately does **not** model the full Phase 6 matrix: mic / Transcribe / Polly don't exist yet, so only the text-agent failures are handled. Every non-ok notice points at the manual portfolio + CV. `session_cap` and `breaker_tripped` are non-retryable → the composer and starter buttons are disabled (a retry this session can't clear them). `AgentPanel` renders this verdict instead of its own local copy map.
- 2026-08-30 — **Partial answer + terminal `error` / dropped connection: keep the streamed text, flag the message `truncated`.** The flag is a boolean on the `ChatMessage`, never folded into `text`, so it never enters the model history sent on the next turn. `Transcript` renders "The answer was cut off there." under it; the error banner carries the reason.
- 2026-08-30 — **Prod circuit-breaker threshold reviewed for the cutover and left unchanged** — `dailyCircuitBreakerThreshold: 500`, `sessionMessageCap: 40` (prod) in `backend/infra/lib/config.ts`, vs `200` / `20` on dev. 500 reasoning calls/UTC-day is ~12–50× a realistic interview (~10–40 messages), so it cannot trip mid-demo; no change needed.
- 2026-08-30 — **`portfolio-api-prod` deployed for the first time** (`cdk deploy portfolio-api-prod --context env=prod`) → `CREATE_COMPLETE` in `eu-central-1`. Creates `portfolio-agent-prod` (Lambda + Function URL `https://tik244apd7xjydojovzps4zz4y0cmgsv.lambda-url.eu-central-1.on.aws/`, CORS locked to the prod Amplify origin), tables `portfolio-{sessions,usage-counters,conversation-logs}-prod`, bucket `portfolio-content-776715560866-prod` — all distinct from dev. This makes "dev/prod separation is real" true, not just scaffolded (Phase 0 last item). `guardrails-*` stacks stay undeployed — empty skeletons, nothing to create.
- 2026-08-30 — **Content synced to the `prod/` S3 prefix** (`npm run content:sync -- --env prod`, 10 files). The prod Lambda still reads its bundled copy (`CONTENT_BUCKET` unset, same as dev) — the sync keeps the S3 seam ready and in step.
- 2026-08-30 — **`VITE_AGENT_URL` set on the Amplify `main` branch** to the prod Function URL (the branch had no env vars before). `dev` → `main` merged and pushed — this is the first time Phase 1 (shell), the two-zone layout, Phase 2 (agent) and the content pass reach production. Verified live on `https://main.daz9bpic9q3nd.amplifyapp.com`.

- 2026-08-30 — **OQ-1 resolved and retired: the Lambda→browser wire format is NDJSON with a fixed frame contract.** One `Content-Type: application/x-ndjson` connection per user turn (the frontend never sees that a "goes deep" turn is two model calls underneath). Frame types: `{"type":"text","delta":"…"}` (append in arrival order); `{"type":"action","name":"reveal_section","args":{"sectionId":"…"}}` (emitted the instant the model yields the tool call, before the answer prose — this is the "reveal + answer at the same time" mechanism); `{"type":"done","usage":{"inputTokens":N,"outputTokens":N},"stopReason":"…"}` (terminal, exactly one, `usage` sums both model calls); `{"type":"error","code":"…","message":"…"}` (terminal alternative to `done`; codes `throttled` / `breaker_tripped` / `session_cap` / `upstream_error` / `internal`; can arrive mid-stream after `text` frames — the frontend handles a partial answer ending in an error). Rules: every turn ends with exactly one terminal frame (`done` XOR `error`, never both, never neither); `get_content` is internal to the Lambda and never appears in the stream; the frontend ignores unknown frame types (extension point). Implemented in `backend/functions/agent/src/types.ts` and `frontend/src/agent/transport.ts`, kept in lockstep by hand.
- 2026-08-30 — **Parallel tool use verified for Claude Haiku 4.5 on Bedrock — a "goes deep" turn is 2 model calls, not 3.** `scripts/verify-parallel-tools.ts` sent a prompt needing both a depth-fetch and a section reveal, 6 iterations at temperature 0: **6/6 emitted `get_content` + `reveal_section` in the same turn** (`stopReason: tool_use`), with **no leading prose** in any of them. So: model call 1 emits both tool calls → the Lambda fires the `reveal_section` action frame immediately and resolves `get_content` internally → model call 2 streams the answer. Plan B from OQ-1 (demote `reveal_section` to an inline text marker to save a call) is **not needed**. Note the "no leading prose" result: the gap between the reveal firing and the answer starting is covered by starting call 2 immediately, not by call-1 prose.
- 2026-08-30 — **`reveal_section` stays a genuine Bedrock tool call** (not an inline marker) — it has real demonstration value as a function-calling example and, with parallel tool use confirmed, costs no extra model call versus the marker approach for the common case.
- 2026-08-30 — **`agent-stack.ts` folded into `api-stack.ts` for Phase 2.** The repo-layout doc anticipated a separate stack for the reasoning Lambda + DynamoDB tables + content bucket; keeping it one stack (`portfolio-api-<env>`) means one deploy unit and one place to look while the surface is small. Split out later if it grows. `identity-stack.ts` is still correctly absent (Phase 4, after OQ-8).
- 2026-08-30 — **Reasoning Lambda runtime is `nodejs22.x`, not `nodejs20.x`** — CDK already flags `nodejs20.x` as deprecated (creation disabled 2027-02-01). Independent of the repo's Node 20 build tooling (`.nvmrc`), which is unchanged. `awslambda.streamifyResponse` (response streaming) works on both.
- 2026-08-30 — **Content is copied into the Lambda bundle for Phase 2** (`NodejsFunction` `commandHooks.afterBundling` `cp -r content/ → asset`), read from the local filesystem by `contentStore.ts`. The S3 path is real and one env var away: set `CONTENT_BUCKET` (+ `CONTENT_PREFIX`) and reads switch to `GetObject` against the bucket `scripts/sync-content.ts` publishes to (`portfolio-content-<account>-<env>`, `dev/` / `prod/` prefixes). `grantRead` on the bucket is already wired so the flip needs no CDK change.
- 2026-08-30 — **In-function token-bucket throttle is per-warm-container, and that's accepted** (`throttle.ts`) — it does not coordinate across concurrent Lambda containers and resets on cold start. Its job is to stop one hot client (a tab in a retry loop) from hammering a single container; the cross-invocation cost bound is the DynamoDB circuit-breaker, which is unaffected.
- 2026-08-30 — **Circuit-breaker uses increment-then-check** (`breaker.ts`) — an atomic `ADD requests :1` with `ReturnValues: UPDATED_NEW`, then refuse if the returned count exceeds the threshold. Race-free for the decision; over-counts by at most the in-flight concurrency, which is immaterial for a soft ceiling well under $25. The tripping request is counted and then refused.
- 2026-08-30 — **Grounding instruction added to the system prompt** — "say only what your reference material and fetched content support; don't invent project names / dates / metrics / stacks". Added after the deployed agent was seen padding a thin seed file with plausible-but-unsourced FlowJob detail. Seed corpus is deliberately minimal (Phase 8 authors the real thing); the instruction keeps the agent honest in the meantime.
- 2026-08-30 — **`VITE_AGENT_URL` is the frontend's only new build-time input** — the reasoning Function URL, set per-branch in Amplify (`dev` branch env var points at `portfolio-api-dev`'s output). Unset → the agent panel renders its "assistant unavailable" state and the manual portfolio carries the experience (a Phase 6 fallback, available early for free). `frontend/.env.example` documents it for local dev.
- 2026-08-30 — **Deployed to dev and verified on the live Amplify staging URL** — `cdk bootstrap` (the account/region had none) then `cdk deploy portfolio-api-dev`. Confirmed against the deployed stack: streamed on-persona answers grounded in the seed corpus, business-before-technical framing, explicit scope-boundary decline for out-of-scope asks, soft "ask him directly" redirect for sensitive ones, no system-prompt leak under an injection attempt, multi-turn history, and `reveal_section` opening the matching section through the existing `revealSection` in `activeSectionStore.ts` (one reveal code path, shared with manual clicks). DynamoDB tables show content + timestamp with zero identifiers.

## 2026-08-30 — Desktop two-zone layout

- 2026-08-30 — **Desktop (≥1024px) restructured from a single scrolling column into a two-zone layout: content zone left (flexible width), agent zone right (fixed 380px), both visible at once, page-level scroll disabled** — implemented as a pure CSS Grid switch (`.app-layout` in `styles/index.css`), not a second DOM tree or a JS-driven layout fork. Below 1024px the grid is inert (`display: block`) and the page is the exact Phase 1 single column; at 1024px the same markup becomes a 3-row grid (header / compact intro / content+agent), with the last row's two cells independently `overflow-y: auto`. Reasoning for each specific call:
  - **Why a CSS-only switch instead of two conditionally-rendered layouts:** the two candidate layouts share stateful children (`PortfolioSections` reads the shared `activeSectionStore`). Mounting it twice — once per layout, toggled by Tailwind's `hidden`/`lg:hidden` — would put two elements with the same `id`/`aria-controls` values in the DOM simultaneously, which is invalid HTML and breaks the `aria-controls` reference. A single mount, repositioned by CSS, has no such collision and needed no new state or a `useMediaQuery` hook.
  - **Why content is left and the agent is right:** two competing conventions exist — "canvas" tools like ChatGPT/Claude put chat left and generated content right, while site-embedded assistants (Intercom, Drift, most AI website widgets) dock right. This product is closer to the second: docs/ARCHITECTURE.md § Product shape is explicit that the manual click-through is "a complete, real alternative... not a dead-end," so the portfolio content gets the primary (left, wider) position, with the agent zone as a persistent, always-visible dock rather than a lesser afterthought or a takeover of the main content position. This also happens to be forced by the DOM-order constraint below, but it was the preferred call independent of that.
  - **Why `grid-template-areas` and not DOM order for the split:** the *mobile* stacking order (intro → agent teaser → browse-manually → sections) had to stay exactly as Phase 1 left it — "nie psuj tego, co działa." Grid areas decouple visual desktop position from source order entirely, so the agent zone's markup can come *before* the content zone's in the DOM (preserving the mobile order and screen-reader/tab order on both breakpoints) while still rendering on the *right* at desktop. Content left / agent right was reachable without reordering anything for mobile's sake.
  - **Why 380px fixed, not a fraction:** wide enough for a comfortable future chat transcript + input (Phase 2) without a min/max-width dance; simple, matches "the simplest thing that actually works." At the 1024px floor this leaves ~644px for the content column before its own padding, still a comfortable reading width, so no extra clamping was needed for the boundary case.
  - **Why the intro (photo/name/blurb) got pulled out of both zones into its own compact grid row** (`components/layout/Intro.tsx`, split out of the old `Hero.tsx`) rather than living inside the agent zone: the agent zone's whole job is holding a chat transcript that Phase 2 will grow into that space unmodified (docs/ROADMAP.md); permanently anchoring unrelated branding content there would mean gutting it again later. Putting intro content inside the *content* zone instead would have meant the content-zone wrapper (intro + sections + footer) had to come before the agent-zone wrapper in DOM order for correct grid nesting — which would have broken the mobile stacking order this whole design was trying to preserve. A third, minimal top row was the option that kept both constraints satisfiable at once. Desktop's intro row is a compact inline strip (small photo, small type) specifically so it doesn't eat meaningfully into the fixed-height budget the two scrolling zones need.
  - **Reveal-in-view without moving the agent zone or the page:** `PortfolioSections` now keeps a ref per section and calls `scrollIntoView({ block: "nearest" })` when `activeSectionId` changes (verified live: only `.layout-content`'s `scrollTop` moves, `.layout-agent` and the page both stay at `0`). `block: "nearest"` was chosen over `"start"` specifically to keep the scroll distance — and therefore the time before the reveal is actually on screen — as short as possible, matching the "reveal lands with the spoken answer" latency budget. `prefers-reduced-motion` is checked explicitly (`behavior: "auto"` when set) rather than relying on browser-default handling of smooth `scrollIntoView`, which isn't consistent enough across browsers to trust.
  - **1024px, matching Tailwind's existing `lg:` breakpoint** used elsewhere in these same components (the mobile-only "Browse manually" button) — one shared breakpoint value, verified live that `.app-layout`'s `display`/`overflow` flips at exactly the same width `lg:hidden` does, so nothing can desync at the edge.

## 2026-08-30 — Phase 1 (frontend shell) built

- 2026-08-30 — **`amplify.yml` simplified: dropped `appRoot: frontend` + `cd .. && npm ci`, build now runs straight from the repo root** — the `appRoot`/`cd ..` construction failed on a real Amplify build: `preBuild` succeeded, but the `build` phase started from a different working directory and walked above the repo root, hitting `ENOENT` on `package.json`. Fixed spec: `npm ci` and `npm run build --workspace=frontend` from root, `artifacts.baseDirectory: frontend/dist`. `main`'s first Amplify job is `FAILED` for this exact reason; the fix commit's job is `SUCCEED`, and `dev`'s first build (created after the fix) was `SUCCEED` from the start.
- 2026-08-30 — **Palette: near-monochrome dark neutral (Tailwind's built-in `neutral` scale) + exactly one warm amber accent (`#e8a33d`)** — deliberately not the blue/purple gradient most AI-product templates default to; the accent is reserved for the agent entry point, active-section state, and links, so it stays meaningful rather than decorative.
- 2026-08-30 — **Type scale: Fraunces (serif, Google Fonts) for headings only, system-ui stack for everything else**, plus a named Tailwind `fontSize` scale (`display`/`h1`/`h2`/`h3`/`body`/`small`) instead of Tailwind's default sizes — one distinctive visual signature without paying webfont cost across all body text, and a scale every component reaches for instead of ad hoc values.
- 2026-08-30 — **Reveal animation: CSS `grid-template-rows` 0fr→1fr + opacity transition, no animation library** — matches the "lightweight animation approach" already called for in `docs/ARCHITECTURE.md` § Frontend; no JS height measurement, no layout thrash, and it collapses to an instant snap under `prefers-reduced-motion` (handled once, globally, in `styles/index.css`).
- 2026-08-30 — **`reveal_section`'s state lives in a plain module-level store (`frontend/src/content/activeSectionStore.ts`), not React Context** — `revealSection(sectionId)` has to be callable from Phase 3's `uiActions.ts` tool-call handler, which is ordinary code outside the component tree and has no component to read Context from; a vanilla subscribe/notify store (read via `useSyncExternalStore`) works identically from a click handler or a tool-call handler. `revealSection()` always opens a section; the click-only `toggleSection()` wraps it with close-on-second-click, since the agent should never accidentally collapse the section it's talking about.

## 2026-08-29 — Bedrock use-case gate resolved

- 2026-08-29 — **Bedrock's per-account "model use case details" gate resolved** — the Anthropic use-case-details form was submitted in the Bedrock console (Model catalog → Submit use case details) for the `portfolio` account (`776715560866`) in `eu-central-1`. `scripts/check-availability.ts` — the regression script that had caught this gate in the first place — now reports **6/6 green**, with Bedrock responding normally through `eu.anthropic.claude-haiku-4-5-20251001-v1:0`. Also corrected an earlier, wrong claim in ARCHITECTURE.md that `authorizationStatus: AUTHORIZED` on the model catalog entry meant no console step was needed — it doesn't; it's a separate signal from whether the model can actually be invoked. Full writeup: [ARCHITECTURE.md § Region](ARCHITECTURE.md#region).

## 2026-08-29 — Phase 0 (code) scaffolded

- 2026-08-29 — **Repo scaffolded per ARCHITECTURE.md § Repository layout, with one deviation: CDK gets its own `backend/infra/package.json`, not a shared `backend/package.json`** — discovered empirically: ts-node's CLI resolves relative script paths (`bin/app.ts`) off the nearest `package.json`, not off `cwd`, so without a `package.json` next to `cdk.json`, `cdk synth` resolved `bin/app.ts` one directory too high and failed with `MODULE_NOT_FOUND`. Root `package.json` workspaces are now `["frontend", "backend/infra"]`. `backend/functions/` doesn't exist yet (Phase 2) so there was nothing to justify a shared umbrella package.
- 2026-08-29 — **`guardrails-stack.ts` kept as an empty skeleton rather than omitted** — chosen over deleting it so the documented repo tree stays intact and there's a named home for a genuinely new future guardrail resource (e.g. WAF per OQ-9, or something reacting to the circuit-breaker). It creates zero AWS resources; the budgets and the SNS topic both already exist manually and must not be duplicated (see 2026-08-29 § Phase 0 AWS results below).
- 2026-08-29 — **`api-stack.ts` scaffolded as an empty skeleton** — the Function URL + response-streaming Lambda it will hold has no code to attach to yet (`handler.ts` is Phase 2). `identity-stack.ts` was deliberately **not created** — Phase 4, after OQ-8 is resolved, per the existing plan.
- 2026-08-29 — **`cdk synth` verified green locally** for both `--context env=dev` and `--context env=prod`, and wired into `.github/workflows/backend-ci.yml`. No AWS credentials needed in CI — `env.account`/`env.region` are fixed values in `config.ts`, no context lookups involved.
- 2026-08-29 — **Local git repo initialized, `main` and `dev` branches created, not pushed anywhere** — no GitHub remote exists yet; nothing was pushed or shared externally.
- 2026-08-29 — **Amplify Hosting connection not attempted via CLI/API — correctly blocked, not skipped by oversight** — the GitHub App authorization Amplify needs is a browser-only OAuth consent flow with no CLI equivalent; the older `--oauth-token` CLI path needs a GitHub personal access token, and entering any such token into a command is off the table regardless of source. There is also no GitHub remote yet to connect to. Recorded as a manual step for later, with exact console instructions, in `docs/ROADMAP.md` § Phase 0.
- 2026-08-29 — **New regression found by `scripts/check-availability.ts`: Bedrock now returns `ResourceNotFoundException: Model use case details have not been submitted for this account`** on the exact `eu.anthropic.claude-haiku-4-5-20251001-v1:0` call that Phase 0's manual verification (below) had already confirmed working. Reproduced 3× (script, direct CLI `converse`, retry) — not transient. Model catalog entry (`list-foundation-models`) still shows `ACTIVE`; this is a separate, account-level entitlement gate. Polly and Transcribe are unaffected (still 5/6 green on the script). Fix is console-only (Bedrock → Model access → submit/resubmit the Anthropic use-case form) — **currently blocking**, flagged in `docs/ROADMAP.md` § Phase 0 and `docs/ARCHITECTURE.md` § Region.

## 2026-08-29 — Phase 0 (AWS) executed manually — real results

- 2026-08-29 — **OQ-2 resolved and retired: Bedrock Haiku 4.5 access confirmed** — reachable only via the EU inference profile `eu.anthropic.claude-haiku-4-5-20251001-v1:0`; the direct in-region ID fails with `ValidationException` (`inferenceTypesSupported` is `["INFERENCE_PROFILE"]` only), and `global.*` also fails (needs a separate Anthropic use-case form). No console click-through needed — Bedrock auto-authorizes Anthropic models. Consequence: the per-env model-ID config must default to the inference-profile form, since Sonnet (the fallback) isn't guaranteed to differ.
- 2026-08-29 — **Polly generative confirmed in `eu-central-1` by real synthesis, not `describe`** — Polish `Ola`/`Ewa` via MP3 synthesis; English generative roster `en-US` (`Danielle`, `Joanna`, `Matthew`, `Ruth`, `Salli`, `Stephen`, `Tiffany`) and `en-GB` (`Amy`, `Brian`) — this roster is the input set for OQ-7.
- 2026-08-29 — **Transcribe streaming confirmed in `eu-central-1`** for `en-US`/`en-GB`/`pl-PL` via a full round trip (Polly synthesis → PCM 16kHz → Transcribe streaming); transcripts matched the original input text.
- 2026-08-29 — **New account-verification pitfall recorded** — a fresh AWS account holds for ~2 hours during which Bedrock (only) returns `AccessDeniedException: "Your account is currently being verified"`; Polly worked immediately on the same account.
- 2026-08-29 — **AWS Budgets provisioned manually via CLI, deliberately kept out of CDK** — `portfolio-monthly-gross-usd25` (the real ceiling) and `portfolio-monthly-net-usd25` (credits-running-out signal), alerting to SNS topic `portfolio-billing-alerts` (`us-east-1`), email confirmed. Reason: the account's 2 free AWS Budgets are already used by these two; each additional budget is a recurring ~$0.60/month, so `guardrails-stack.ts` must never create `AWS::Budgets` resources.
- 2026-08-29 — **CloudWatch billing alarm rejected outright, not deferred** — `EstimatedCharges` is computed post-credit (would duplicate the net-spend budget), is only published in the payer account under consolidated billing (needs a `LinkedAccount` dimension, i.e. a different account entirely), and needs root-level "Receive Billing Alerts" plus tolerates ~24h metric lag. The gross AWS Budget is the second independent signal instead; the DynamoDB circuit-breaker remains the real-time protection.
- 2026-08-29 — **Credit-sharing context recorded (not fully resolved)** — credit sharing for the `portfolio` account is confirmed `ENABLED` via the API; balance and expiry are unknown (reading them needs "IAM access to billing" enabled at the management-account level, not yet done). The org runs ~$84/month gross, currently fully covered by credits. Bedrock usage is confirmed credit-covered; Polly/Transcribe coverage is unconfirmed for lack of usage history so far.
- 2026-08-29 — **Account access details recorded** — account `portfolio` (`776715560866`); this organization has no AWS Identity Center, so access is via CLI profile `portfolio`, which assumes `OrganizationAccountAccessRole` from the `rj.grajewski-admin` profile; default region `eu-central-1`.

## 2026-08-29 — Architecture review follow-ups

- 2026-08-29 — **Reasoning path resolved: Lambda Function URL with response streaming** (see [ARCHITECTURE.md § Real-time media transport](ARCHITECTURE.md#real-time-media-transport)) — API Gateway REST buffers full responses and HTTP API doesn't support response streaming either, so Function URL is the only option that satisfies Phase 2's streaming requirement, not merely the preferred one. Consequence: throttling now lives entirely in the function (token bucket, no API Gateway usage-plans available); CORS is set on the Function URL directly; CloudFront stays deferred.
- 2026-08-29 — **New open question (OQ-1, highest priority): tool-use sequencing vs. streaming, and the Lambda→browser wire format** — a `tool_use` block ends the assistant's turn, so a full answer after a tool result needs a second model call, risking dead air between `reveal_section` firing and the spoken answer arriving. Adopted approach: `reveal_section` stays a real tool (two model calls per turn accepted); verify in Phase 2 whether Haiku 4.5 can emit `get_content` + `reveal_section` in parallel in one turn; plan B is a structural marker in the text stream instead of a real tool. Wire format proposed as NDJSON (`{type: text|action|done}`), to be settled before `handler.ts` is written.
- 2026-08-29 — **Corrected: the per-session message cap is not an abuse control** — `sessionId` is client-generated and trivially rotated, so the cap only bounds accidental overuse (a stuck retry loop), not deliberate abuse. The daily circuit-breaker is the only real cost backstop. Removed the earlier claim that the client-side cap gates STT/TTS abuse, and reconciled the "enforced server-side" wording with this limitation.
- 2026-08-29 — **OQ-8 (Cognito guest-role abuse ceiling) reclassified as blocking** — must be resolved *before* the Identity Pool is provisioned to production, not just "during Phase 4". Guest credentials are pullable by any script without loading the page, there is currently no server-side quota on the Transcribe/Polly path, and Polly generative runs ~$30/1M characters — enough to breach the ceiling in an afternoon of deliberate abuse.
- 2026-08-29 — **"Not templated / design-forward" visual bar formally moved from Phase 1's exit criteria to Phase 9** — resolves a conflict with the README's "does not need to be fully polished" framing for the Tuesday demo. Phase 1 keeps a timeboxed design pass only; the full bar is Phase 9's to clear.

## 2026-08-29 — Initial architecture

- 2026-08-29 — **Project documentation lives under `docs/`**; root keeps only `README.md` as the entry point — keeps the root free for code/config once scaffolding starts, and gives one place for architecture, roadmap, decisions, specs, and runbooks.
- 2026-08-29 — **Product = single-page portfolio with 3 entry points** (CV download, manual click-through, AI agent) driving the same content/UI — the agent is an alternate navigation of the portfolio, not a bolted-on feature.
- 2026-08-29 — **Hard cost ceiling: ~100 PLN/month (~$25) worst case**, architecture must scale toward zero when idle — it's a personal portfolio that sits unvisited for weeks.
- 2026-08-29 — **Banned OpenSearch Serverless (classic) as a vector store** — ~4 OCU minimum ≈ ~$700/mo with zero traffic, AWS defaults you into it from the Knowledge Base wizard, and deleting the KB doesn't delete the collection.
- 2026-08-29 — **No always-on compute** — Lambda + API Gateway / Function URLs only; no provisioned capacity, no persistent servers.
- 2026-08-29 — **AWS Budget alarm is Phase 0**, not an afterthought — configured before any feature work.
- 2026-08-29 — **Chose AWS Amplify over Vercel/GitHub Pages** — the narrative value of a second real Bedrock/AWS proof point outweighs the low, already-assessed config risk; funds already exist in the org account.
- 2026-08-29 — **Branch-based deploys: `dev` → staging, `main` → production** — also gives dev/prod separation from day one (a FlowJob/Rhymind lesson).
- 2026-08-29 — **Region: `eu-central-1` (Frankfurt)** — lowest latency from Poland for a voice app; supports Polly generative, Transcribe streaming, Bedrock (verify model specifics in Phase 0).
- 2026-08-29 — **Frontend: Vite + React + TypeScript static SPA, Tailwind** — React was the brief default; Vite over Next.js because there's no SSR need and the "no route changes" rule means no router.
- 2026-08-29 — **Chose Claude Haiku 4.5 on Bedrock over higher tiers** — voice latency and cost; answering CV questions with supplied context doesn't need frontier reasoning. Sonnet is the fallback if quality falls short; Opus/Fable are ruled out (~10× cost, slower, unjustified).
- 2026-08-29 — **Bedrock requests stream** — first tokens drive first audio and the section reveal; perceived latency is what matters.
- 2026-08-29 — **Chose Polly generative over ElevenLabs** — Polly generative quality judged acceptable after a listening test; ElevenLabs is 3–6× costlier with subscription-tier pricing that breaks the ceiling in idle months, and adds a non-AWS vendor/key.
- 2026-08-29 — **TTS = Polly generative engine tier specifically** (not standard/neural) — meaningfully more natural, and supports streaming so audio can start before generation finishes (exact streaming API to be verified).
- 2026-08-29 — **STT = Amazon Transcribe streaming**; **rejected the Web Speech API** — too limited/inconsistent, and the portfolio should showcase advanced, not basic browser features.
- 2026-08-29 — **Chose hybrid tool-fetch (Option C) over vector retrieval** — the corpus is a small, known, enumerable topic set with business/technical layers; the agent needs to *choose*, not *search*. ~5× cheaper and lower-latency than full-context.
- 2026-08-29 — **Considered and parked full-context + prompt caching (Option A)** — TTFT scales with corpus size and Bedrock's ~5-min cache TTL means a paused visitor pays full price on the next turn.
- 2026-08-29 — **S3 Vectors is the sanctioned migration path** if the corpus outgrows enumeration — additive behind the existing `get_content` tool, no provisioned compute, ~cents/month at this scale.
- 2026-08-29 — **Rejected non-AWS vector DBs** — no meaningful saving at this scale, Pinecone's ~$50/mo minimum alone breaches the ceiling, and each adds a vendor + API key + cross-cloud hop.
- 2026-08-29 — **Agentic UI = structured action alongside the answer** (`reveal_section`), executed by the frontend against the same components a manual click drives — single page, no new tabs, no route changes, reveal and answer simultaneous.
- 2026-08-29 — **Real-time media plan: browser talks directly to Polly/Transcribe** via a tightly-scoped Cognito Identity Pool guest role; the Lambda owns reasoning + session cap + circuit-breaker + logging; Bedrock is never called client-side. (Plan B: API Gateway WebSocket proxy. To be validated — OQ-3.)
- 2026-08-29 — **Data stores: DynamoDB on-demand** for session caps, the daily spend counter, and conversation logs; S3 for the content corpus (dev/prod prefixes). Chosen for "scales to zero", not cost.
- 2026-08-29 — **Real-time circuit-breaker is a self-managed DynamoDB counter**, not AWS Budgets — Budgets/Cost Explorer data lags hours and can't stop a spike in progress. Over threshold → degrade to text-only or disable the agent.
- 2026-08-29 — **WAF deferred for MVP** — the ~$5/mo web-ACL floor conflicts with "scale toward zero"; rely on throttling + session cap + breaker, add WAF only if abuse appears.
- 2026-08-29 — **Logging: conversation content + timestamps only, zero identifiers** — random non-identifying `sessionId`; purpose is topic insight for the owner, not visitor tracking. Admin dashboard deferred.
- 2026-08-29 — **Bilingual EN/PL via an explicit UI language toggle** — one choice drives Transcribe language, Polly voice, and the response-language instruction; corpus stays English-only and is translated at generation time, never duplicated.
- 2026-08-29 — **Accept distinct Polly voice identities per language** — no single generative polyglot voice cleanly covers EN+PL; pair an EN generative female voice with PL `Ola`/`Ewa`. Revisit if a suitable polyglot voice ships.
- 2026-08-29 — **Infra-as-code = AWS CDK (TypeScript)** for Lambda / DynamoDB / API / Cognito / IAM / Budgets; the Amplify Hosting app is connected via console to keep the frontend build path simple. Backend deploys stay manual so infra changes are deliberate.
- 2026-08-29 — **Guardrails: out-of-scope = explicitly name the scope boundary; sensitive = soft redirect to "ask him directly"; injection = hold persona/scope, never leak the system prompt.** Injection handling is treated as part of the demo and gets a dedicated test phase.
- 2026-08-29 — **`content/` corpus is treated as data, not instructions** — the system prompt says so explicitly, so a poisoned content file can't redirect the agent.
- 2026-08-29 — **Build order: MVP first with seed/placeholder content, full content authoring as an explicit post-MVP phase** — lets the pipeline be tested end to end immediately; content is still on the critical path for Tuesday.
- 2026-08-29 — **Deferred (not decided now):** landing "about me" copy, the conversation opener script/example questions, whether portfolio-as-a-project is proactively surfaced, the admin dashboard, the custom domain, fine-grained Amplify build specifics.
